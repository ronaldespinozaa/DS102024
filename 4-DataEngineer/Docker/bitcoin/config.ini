[api]
# Real-time data configuration
vs_currency=usd
# Retry configuration
max_retries=3
retry_delay=2
use_mock_on_failure=true

[database]
schema=bitcoin
table=prices

[etl]
batch_size=1000
incremental=true
schedule=2min